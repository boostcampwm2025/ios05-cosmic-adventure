# Cosmic Adventure

A game about a spaceship exploring the universe.

## Development

To run the project, you need to have Tuist installed. You can install it with the following command:

```bash
brew install tuist
```

Then, you can run the project with the following command:

```bash
tuist run
```
