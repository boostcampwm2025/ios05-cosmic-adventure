import ProjectDescription
 
let project = Project(
    name: "{{ name }}",
    targets: [
        .target(
            name: "{{ name }}",
            destinations: .iOS,
            product: .framework,
            bundleId: "dev.tuist.iOS.{{ name }}",
            infoPlist: .default,
            sources: [
                "Sources/**"
            ],
            dependencies: []
        )
    ]
)
